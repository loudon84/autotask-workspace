import {
  listFlows,
  publishFlowVersion,
  uploadFlowPackage,
  validateFlowVersion,
} from "./handlers";

export const rpaEngine = {
  listFlows,
  publishFlowVersion,
  uploadFlowPackage,
  validateFlowVersion,
};
