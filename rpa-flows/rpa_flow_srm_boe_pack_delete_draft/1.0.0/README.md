# 删除 SRM 发票箱单草稿 1.0.0

作废 2.2：按发票箱单流水号搜列表。已是「暂无数据」则成功（不必删）。还在则勾冻结列复选框（主表勾选被盖住），点影刀「删除-草稿单删除」，确认框「确定」，再搜一次确认没有。

选择器来自 `project-docs/prd/boe/影刀-京东方-selectorsV2.xml`（删除-草稿单删除 / 删除确认框 / 确定-删除 / 暂无数据-列表）。
