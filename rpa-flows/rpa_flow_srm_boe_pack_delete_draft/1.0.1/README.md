# 删除 SRM 发票箱单草稿 1.0.1

作废 2.2：按发票箱单流水号搜列表。已是「暂无数据」则成功（不必删）。还在则勾冻结列复选框，点影刀「删除-草稿单删除」，确认框「确定」，**再点「搜 索」刷新列表**，用「暂无数据」判断成功（不要看搜索框里还留着的流水号）。

1.0.0 删完没等列表刷新就读旧行，会报 `BOE_DRAFT_STILL_PRESENT`。重试仍走本包：先搜，已是「暂无数据」则 `alreadyMissing`，不再点删除。

选择器来自 `project-docs/prd/boe/影刀-京东方-selectorsV2.xml`（删除-草稿单删除 / 确定-删除 / 搜 索-发票箱单列表 / 暂无数据-列表）。
