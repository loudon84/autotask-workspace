# SRM 回签状态只读探测（rpa_flow_srm_check_reply_status 1.1.2）

> **适用门户：正式门户**（`https://supplier.tiandy.com`，无 `data-rpa`）。**不能绑演示门户**。演示门户继续用 `1.0.1`。

登录 → 打开订单详情 → 读取回复状态标签 → 输出 `replyStatus`。**禁止**点击签章或任何写操作。

## 1.1.2 变更

打开详情与建单 1.2.15 / 下合同 1.3.0 相同：按订单号对齐行号，点冻结列里可见的「详情」，不用 `force`。进详情后认「查看签章」或「导出订单明细」，不再用列表上的 `.el-tag` 冒充详情。

## 1.1.1 / 1.1.0

正式站专用，无 `data-rpa`，列表 `#/order/list`。
