# SRM 扫单 Flow（rpa_flow_srm_scan_pending_orders 1.0.2）

> **适用门户：演示门户**（`http://192.168.102.247:3000`，带 `data-rpa`）。**不能绑正式门户**。正式只读扫单仍是 `1.1.0`。回退演示文件名对照表用 `1.0.1`。

## 1.0.2 变更

- 登录验证码改为本机 `ddddocr` 读图，**不再用** `code01.png → mp3s` 文件名对照。
- 最多 3 次；失败抛可重试错误，不停成待人工。

## 用途

与 1.0.1 相同：只读扫待签章订单。
