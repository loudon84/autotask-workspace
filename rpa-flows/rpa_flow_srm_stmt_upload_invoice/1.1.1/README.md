# SRM 扫描发票（rpa_flow_srm_stmt_upload_invoice 1.1.1）

> **适用门户：正式门户**（`https://supplier.tiandy.com`，无 `data-rpa`）。**不能绑演示门户**。演示继续用 `1.0.6`。

相对 1.1.0：点「收货应付」认冻结列 `el-table__fixed-body-wrapper` 里可见按钮（与建单点「详情」同一类问题）。匹配成功会把日期/金额写进步骤日志。
