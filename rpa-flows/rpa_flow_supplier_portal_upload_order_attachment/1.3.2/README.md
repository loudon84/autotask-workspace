# 供应商门户双方签章合同上传 Flow

版本：`1.3.2`

> **适用门户：正式门户**（`https://supplier.tiandy.com`，无 `data-rpa`）。演示站继续用 `1.2.5`。本包只下载已回签合同并上传到我们的测试 SDMS，**不点** SRM 保存/签章/提交。Binding **不要**设 `dryRun`（那会拦住 SDMS 上传）。

列表按 **回复状态=已回签 + 订单编号** 查询，有行再进详情点「查看签章」下载。进了详情就不再读回复状态。

## 1.3.2 变更（相对 1.3.0）

搜索条件就是已回签和单号。能打开详情即已回签；详情只用来点「查看签章」，不找 `.el-tag`。

## 输入契约

```json
{
  "po_no": "POJS2607170008",
  "username": "Auth登录工号"
}
```
